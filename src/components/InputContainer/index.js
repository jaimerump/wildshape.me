import styled from 'styled-components';

// import cssVars from '../../cssVariables';

export const InputContainer = styled.div.attrs({ className: 'input__container' })`
  margin: 1em;
`;

export default InputContainer;
